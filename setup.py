from setuptools import find_packages, setup

setup(
    name='src',
    packages=find_packages(),
    version='0.1.0',
    description='this project used place api and geocode api to filter the closed stores',
    author='Allen Ren',
    license='',
)
